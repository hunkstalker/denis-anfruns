---
import SocialLinks from "../components/SocialLinks.astro";
import PoweredAstro from '../components/PoweredAstro.astro';
import { getLangFromUrl, useTranslations } from '../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const year = new Date().getFullYear();
---
<footer class="text-center mb-16">
  <SocialLinks />
  <p class="mt-6 mx-16 sm:mx-20 mb-24 leading-6">&copy; {year} Denis Anfruns Millán.<br class="block md:hidden">&nbsp;{t('footer.rights')}</p>
  <PoweredAstro />
</footer>