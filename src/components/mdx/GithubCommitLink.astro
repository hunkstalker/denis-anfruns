---
import GithubIcon from '../../icons/github.svg';
import ExternalLink from '../../icons/external-link.svg';

interface Props {
  href: string;
  title: string;
}

const { href, title } = Astro.props;
---

<a 
  href={href}
  target="_blank"
  rel="noopener noreferrer"
  class="inline-flex items-center gap-5 px-5 py-3 rounded-md bg-zinc-900 dark:bg-zinc-100 hover:bg-zinc-800 dark:hover:bg-zinc-200 text-white dark:text-zinc-900 transition-colors no-underline group"
>
  <GithubIcon class="w-5 h-5 text-zinc-100 dark:text-zinc-900" />
  <span class="text-base border-none font-medium">
    {title}
  </span>
  <ExternalLink class="w-5 h-5 text-zinc-400 group-hover:text-zinc-200 dark:text-zinc-500 dark:group-hover:text-zinc-700 transition-colors" />
</a>
