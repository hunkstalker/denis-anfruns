---
title: "Tipus d'Unió a TypeScript"
pubDate: 2025-11-25
lang: "ca"
---

import CodeTabs from '../../../components/mdx/CodeTabs.astro';

Defineix variables que poden ser de múltiples tipus. Útil per a estats o respostes d'API.

<CodeTabs 
  items={[
    { 
      label: 'TypeScript', 
      code: `type Estat = 'carregant' | 'exit' | 'error';
      
function processar(estat: Estat) {
  if (estat === 'error') {
    console.error('Alguna cosa ha anat malament');
  }
}`, 
      lang: 'typescript' 
    }
  ]}
/>

Això evita "nombres màgics" i cadenes de text soltes.
