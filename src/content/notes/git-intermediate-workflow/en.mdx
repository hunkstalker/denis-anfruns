---
title: Flujo de trabajo intermedio con Git
description: Gestión de ramas, stash y visualización del historial para el día a día.
pubDate: '2025-12-13T17:00:00Z'
tags: ['git']
lang: en
icon: git
new: true
---

import CodeTabs from '../../../components/mdx/CodeTabs.astro'

Once you master `commit` and `push`, it's time to upgrade your workflow to handle branches, temporary changes, and history reviews.

### 1. Branch Management (Modern vs Classic)

Creating and switching branches is essential to keep `main` clean.

<CodeTabs
	items={[
		{
			label: 'Modern (Recommended)',
			code: `git switch -c feature/new-feature   # Create and switch
git switch main                     # Return to main`,
			lang: 'bash',
		},
		{
			label: 'Classic',
			code: `git checkout -b feature/new-feature # Create and switch
git checkout main                   # Return to main`,
			lang: 'bash',
		},
	]}
/>

### 2. Temporary Storage (Stash)

Have halfway changes but need to switch branches urgently? Don't commit broken code, put it in your "pocket".

<CodeTabs
	items={[
		{
			label: 'Bash',
			code: `git stash                   # Save changes and clean directory
git stash pop               # Recover latest saved changes
git stash list              # List all stashed changes`,
			lang: 'bash',
		},
	]}
/>

### 3. Readable History

Viewing the default history log can be overwhelming.

<CodeTabs
	items={[
		{
			label: 'Bash',
			code: `git log --oneline --graph --all  # Compact visual tree`,
			lang: 'bash',
		},
	]}
/>

### 4. Undoing Changes

<CodeTabs
	items={[
		{
			label: 'Bash',
			code: `git restore file.txt           # Discard local changes
git restore --staged file.txt  # Unstage changes`,
			lang: 'bash',
		},
	]}
/>
